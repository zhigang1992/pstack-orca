---
description: Enter poteto-mode for this task. Reads the poteto-mode skill, matches a playbook, and runs it with rigor.
argument-hint: "<task description>"
---

Read the `poteto-mode` skill's `SKILL.md` in full (it ships in the pstack-orca package's
`skills/poteto-mode/` directory; use your skill list to locate it), including its inline
Principles index, then apply it to this task:

$@

Match the task to one of its playbooks, copy the playbook's steps into your todo list
verbatim, and run them. If no playbook fits, route to the **figure-it-out** skill as the
skill directs.
